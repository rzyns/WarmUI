import { describe } from "vitest";

describe.skip("ModelDB", async (test) => {
    test("something else", async ({ expect }) => {
        // nothing for now
        expect(true).toBe(true);
    });
});
